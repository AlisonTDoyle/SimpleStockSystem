import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'simple-stock-system',
  webDir: 'dist/simple-stock-system/browser'
};

export default config;
